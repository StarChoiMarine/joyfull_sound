# 기쁜소리 프로젝트 브리프

## 제품 정의

기쁜소리(`joyfull_sound`, 가칭)는 저녁예배 찬양팀 14명이 예배 밖에서도 서로의 최신 기도제목을 나누고, 2개월마다 익명 기도 파트너인 **기니또**를 배정받아 실제 기도를 이어가도록 돕는 모바일 우선 공동체 웹앱이다.

이 제품은 추첨기나 기도 실적 인증 도구가 아니다. 감시, 순위, 경쟁, 생색내기를 제거하고 기도와 묵상, 찬양을 통해 일상에서도 공동체로 연결되는 조용한 동기를 제공한다.

## 1차 MVP의 핵심 경험

1. 초대받은 멤버가 로그인한다.
2. 자신의 기도제목을 추가하고 수정하며 완료 처리한다.
3. 현재 기니또는 자신이 맡은 대상자의 최신 기도제목만 확인한다.
4. 멤버는 현재 기도 대상자와 그 사람의 최신 기도제목을 본다.
5. 기도 후 `기기완!`을 누른다.
6. 날짜별 `기도의 밤하늘`에 익명의 별이 생긴다.
7. 대상자 및 나의 기니또와 익명 편지를 주고받는다.

## 기도의 밤하늘 원칙

- 한 멤버가 그날 처음 기도하면 별 하나를 생성한다.
- 같은 날 반복 기도하면 별을 추가하지 않고 기존 별의 밝기를 최대 3단계까지 높인다.
- 공개 정보는 기도 시각과 익명 문구뿐이며 사용자 ID, 이름, 개인별 횟수, 순위, 연속 출석을 노출하지 않는다.
- 14명 전원이 하루 한 번 이상 기도하면 `오늘은 모두가 기도한 날이에요!` 메시지와 절제된 축하 효과를 표시한다.
- 별의 위치와 크기는 날짜·익명 공개 식별자를 기반으로 안정적으로 생성한다.
- DB 내부에는 중복 판정용 사용자 연결을 유지하되, 클라이언트 공개 조회는 안전한 RPC 또는 제한된 view만 사용한다.

## 사용자와 권한

- `member`: 본인 프로필과 기도제목을 관리하고, 현재 배정된 대상자의 활성 기도제목만 열람한다. 본인의 배정 관계만 확인한다.
- `admin`: 초대와 멤버, 2개월 배정 시즌을 관리한다. 확정 전 배정 미리보기를 검증한다. 기도제목 본문 접근은 운영상 꼭 필요한 범위로 제한한다.
- 외부 사용자는 OAuth 로그인만으로 가입할 수 없다. 관리자가 발급한 1회용 초대와 최초 로그인 계정을 결합해야 한다.

## 인증 방향

운영 인증은 Supabase Kakao OAuth와 14명 각각의 1회용 초대 토큰을 사용한다. 자격증명 준비 전에는 운영 보안처럼 가장하지 않는 `DEV DEMO` 모드를 제공하며, 인증 서비스는 Kakao OAuth 또는 이메일 OTP로 교체 가능한 인터페이스로 분리한다. 비밀키와 민감한 기도 데이터는 소스나 브라우저 저장소에 저장하지 않는다.

## 기술 방향

- React + TypeScript + Vite 기반 모바일 PWA
- Supabase Auth, Postgres, RLS, 추후 Storage와 선택적 Realtime
- 브라우저 표준 및 서비스 레이어를 사용해 향후 Capacitor 패키징 가능성 유지
- 배포 환경 변수는 `.env.example`에 이름만 기록하고 실제 값은 커밋하지 않음
- DB 변경은 migration으로 관리하며 실제 데이터 입력 전 RLS와 권한 테스트 필수

## 디자인 원칙

- Pretendard Variable, 모바일 전체 화면, iPhone safe area
- 짙은 네이비와 보라/라벤더 그라데이션, 별·숲·빛의 차분한 영성
- 귀엽지만 유아적이지 않은 카드, 큰 둥근 주요 버튼, 짧은 문장
- 하단 고정 내비게이션: 홈 / 기도 / 밤하늘 / 우편함 / 내 정보
- 참고 이미지의 브랜드나 캐릭터는 복제하지 않고 분위기와 UI 원칙만 반영
- 접근 가능한 대비, 44px 이상의 터치 영역, 로딩·빈 상태·오류·권한 없음 제공

## 1차 제외 범위

MP3 기도 BGM/타이머, 공동체 기도제목, 찬양 콘티·악보·연습 음원, 큐티로그, 푸시 알림, 앱스토어 배포는 후속 단계다. 현재 구조는 이 기능을 확장할 수 있게 하되 MVP를 지연시키지 않는다.

## 완료와 외부 의존성

Codex는 앱 코드, UI, DB migration/RLS, 테스트, PWA와 배포 준비 문서를 담당한다. 사용자는 Supabase 및 Kakao Developers 프로젝트 생성, 환경 변수 설정, 실제 14명 명단과 관리자 지정, 익명·개인정보 정책 확인, 실제 휴대폰 테스트를 담당한다. 비밀키는 채팅이나 저장소에 공유하지 않는다.
